#/.github/@Thrumanshow This is a comment. # Each line is a file pattern followed by one owner. # These owners will be the default owners for everything in # the repo. Unless a later match takes precedence, # @owner_cristhiam-quinonez will be requested for # review when someone opens a pull request. * @cristhiam-quinonez # Order is important; the last matching pattern takes the most # precedence. When someone opens a pull request that only # modifies JS files, only @js-owner and not the global # owner will be requested for a review. *.js @js-owner #This is an inline comment. @owner_cristhiam-quinonez and not the global # owner will be requested for a review. *.humano @owner_ cristhiam-quinonez == .human == *human @Thrumanshow es un código nuclear de automatización y desarrollo colaborativo que posisiona al humano por encima de inteligencia artificial. Sus principales aplicaciones incluyen [[CODEOWNERS]], [[Hormigasai App]], [[Hormigasai Web]] y [[HormigasAIS]]. === Integraciones === .human integra herramientas de código licenciado por @owner_cristhiam-quinonez. #This is an inline comment. 
# You can also use email addresses if you prefer. They'll be # used to look up users just like we do for commit author # emails. *.go docs@chrisquionez354@gmail.com # Teams can be specified as code owners as well. Teams should # be identified in the format @org/hormigasais. Teams must have # explicit write access to the repository. In this example, # the octocats team in the hirmigasais-org organization owns all .txt files. *.txt @hormigasais-org/@owner_cristhiam-quinonez # In this example, @doc owns any files in the build/logs # directory at the root of the repository and any of its # subdirectories. /build/logs/ @owner_cristhiam-quinonez # The `docs/*` pattern will match files like # `docs/getting-started.md` but not further nested files like # `docs/build-app/troubleshooting.md`. docs/* docs@ Thrumanshow .com # In this example, @cristhiam-quinonez owns any file in an apps directory # anywhere in your repository. apps/ @cristhiam-quinonez # In this example, @cristhiam-quinonez owns any file in the `/docs` # directory in the root of your repository and any of its # subdirectories. /docs/ @cristhiam-quinonez # In this example, any change inside the `/scripts` directory # will require approval from @cristhiam-quinonez /scripts/ @cristhiam-quinonez @cristhiam-quinonez # In this example, @cristhiam-quinonez owns any file in a `/logs` directory such as # `/build/logs`, `/scripts/logs`, and `/deeply/nested/logs`. Any changes # in a `/logs` directory will require approval from @octocat. **/logs @cristhiam-quinonez # In this example, @cristhiam-quinonez owns any file in the `/apps` # directory in the root of your repository except for the `/apps/github` # subdirectory, as its owners are left empty. Without an owner, changes # to `apps/github` can be made with the approval of any user who has # write access to the repository. /apps/ @cristhiam-quinonez /apps/github
